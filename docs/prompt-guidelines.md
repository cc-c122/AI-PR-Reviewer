# Prompt Guidelines

Prompts must be evidence-first and constrained to provided context.

Required rules:

1. Do not invent missing project behavior.
2. Cite code evidence for every finding.
3. Mark uncertain results as requiring human confirmation.
4. Avoid generic style comments without a concrete location and impact.
5. Return JSON that matches the configured schema.
